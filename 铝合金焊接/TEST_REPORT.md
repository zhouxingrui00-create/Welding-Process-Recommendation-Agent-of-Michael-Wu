# LLM 架构升级测试记录

测试时间：2026-09-09，Asia/Shanghai。

## 完整解释超时修复（17:12 起）

用户反馈连接测试成功，但工艺解释仍显示“AI解释模块不可用”。日志显示 17:08:26 和 17:10:44 的正式云端调用均在约 30.17 秒超时，随后 Ollama 连接失败。根因是原先 30 秒的正式生成超时不足；简短 `OK` 连接测试不能代表完整解释耗时。

- 将 `config/model_config.yaml` 中正式云端读取超时调整为 120 秒，连接测试仍独立使用 15 秒。
- 使用最近一次失败推荐记录的输入，经原有 Agent 全流程真实复验：`qwen3.8-max` 在 **50023.01 ms** 后成功返回，**703 字解释实际被采用**，通过原有解释安全校验；材料、方法、参数、风险、知识、验证建议与冲突字段和规则基线完全一致。
- 页面重跑时自动读取配置变化，保持旧推荐结果，并提示用户重新生成；无配置变化时保留已有模型状态。
- 推荐结果保存本次模型尝试的脱敏状态，页面分别展示云端超时、本地连接失败等具体原因及耗时。
- 新增回归测试覆盖配置自动重载、旧结果保留、重新生成、失败原因显示、长生成超时与短测试超时独立、单次调用记录无串用。
- 全套测试：**55 passed in 2.19s**。

本次未修改规则决策、知识库或安全校验逻辑。当前页面已显示的旧结果不会自行变成新解释，需要再次点击“生成焊接工艺推荐”。

## 最新真实服务复验（17:04）

已将用户指定的阿里云百炼地址 `https://dashscope.aliyuncs.com/compatible-mode/v1` 和模型 `qwen3.8-max` 写入 `.env`，保留用户提供的 Key。

- 真实 API 连接测试成功，耗时 1390.05 ms。
- 通过 `LLMRouter.generate()` 进行真实调用成功，模型为 `qwen3.8-max`，收到 `OK`，耗时 3234.06 ms。
- 在内存中使用故意错误的测试 Key，真实服务返回认证失败；Router 随后实际尝试 Ollama。本地服务无法连接，最终返回“AI解释模块不可用”。测试未更改 `.env` 中的有效 Key。
- 重新运行全套测试：**53 passed in 1.89s**。原有规则推荐和自动 fallback 回归通过。
- Ollama 真实生成验收仍未通过：本地 `127.0.0.1:11434` 服务无法连接。API 已可用，不影响当前使用联网解释。

本次真实调用只发送中性短提示词，不发送焊接数据。应用若已运行，进入“模型设置”点击“重新加载配置”即可使用新配置。

## 实施范围

- Agent 默认依赖统一 `LLMRouter`；API 和 Ollama 客户端均位于 `services/llm/`。
- 增加 `.env` 配置、`config/model_config.yaml`、模型设置页面、连接测试脚本和独立模型调用日志。
- 云端失败自动转本地，两端失败返回“AI解释模块不可用”，继续原有规则解释。
- 原解释提示词、推荐逻辑及数值安全校验保持不变。未进行模型训练。

## 自动化测试

命令：`.\.venv\Scripts\python.exe -m pytest -q`

升级前：17 passed。升级后：**53 passed in 1.82s**。

覆盖以下行为：

- OpenAI-compatible 请求路径、模型、Bearer 认证、非流式请求与响应解析。
- Ollama 请求路径、模型、非流式生成和响应解析。
- 云端优先；401/403、429、500、重定向、网络断开、超时、无效 JSON、无效结构、空响应均转本地。
- 本地失败或两端失败后保留规则推荐；下一次调用恢复优先尝试云端。
- 仅本地模式、关闭本地 fallback、缺少 Key、配置异常、无效超时。
- `.env` / 进程环境 / YAML 优先级、重新加载及密钥不进入配置 repr、错误状态和模型日志。
- 调用日志包含模型、时间、成功/失败和响应耗时；日志写入失败不阻断模型返回。
- 云端解释成功或被原安全校验拒绝时，结构化推荐结果不变。
- Streamlit 主页面、设置页面、连接失败和成功状态、配置重载、跨页保留模型状态和推荐结果。
- 使用本地 HTTP 测试服务完成真实 HTTP 传输，验证两种协议和错误 Key 触发 fallback；该服务是协议测试替身，不是真实 LLM。

另已通过 Python compileall 语法检查。文件 SHA-256 对比确认：规则引擎、安全校验、其余原有 agent 文件、data、knowledge 和 vector_db 文件内容未变；recommender.py 仅调整模型依赖导入、类型及默认构造。

## 首次实际服务检查（历史记录，配置前）

命令：`.\.venv\Scripts\python.exe scripts/check_llm.py`

最后检查时间：2026-09-09 16:53:55 +08:00。

| 服务 | 实际结果 | 原因 |
| --- | --- | --- |
| 联网 API | 未通过真实调用验收 | `.env` 的 Base URL、Key 和模型尚未填写；客户端未发送网络请求 |
| Ollama | 未通过真实调用验收 | `http://127.0.0.1:11434` 无法连接；目标模型为 `qwen2.5:7b-instruct-q4_K_M` |
| 自动 fallback | 自动化测试通过 | 覆盖云端失败转本地、本地也失败转规则，以及云端恢复 |
| 原有 Agent | 回归测试通过 | 原有 17 项测试通过，另增加结构化输出与页面状态回归检查 |

脚本退出码为 1，准确表示至少一端服务未就绪。不能将协议模拟测试通过视为真实 API / Ollama 模型已调用成功。

完成真实验收：填写 `.env`，启动配置对应的 Ollama 服务并确保目标模型已安装，然后在“模型设置”页面重新加载配置并测试连接，或重新执行 `scripts/check_llm.py`。该脚本仅发送中性短提示词，不发送焊接上下文。
