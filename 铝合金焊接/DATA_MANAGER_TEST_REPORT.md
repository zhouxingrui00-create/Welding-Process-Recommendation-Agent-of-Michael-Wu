# 科研数据管理框架验收

执行日期：2026-09-12。阶段：数据基础设施，不训练模型、不预测、不优化、不生成实验测量数据。

## 实际 CSV 导入

已在项目虚拟环境中执行：

```powershell
.\.venv\Scripts\python.exe scripts\import_dataset.py data\templates\demo_schema_only.csv --dataset demo
```

| 项目 | 实际结果 |
|---|---|
| 文件 | `data/templates/demo_schema_only.csv` |
| 仓库 | demo |
| dataset_version | v000001 |
| 更新时间 UTC | 2026-09-12T06:35:34.224973+00:00 |
| 更新时间 北京时间 | 2026-09-12 14:35:34 |
| 数据数量 / 新增数量 | 1 / 1 |
| 记录性质 | schema_test；无任何实验测量 |
| 标准化缺失字段 | 14 个，全部保留 null |
| 核心字段完整度 | 17.65%（3 / 17） |
| 材料分布 | 6A01：1 |
| 焊接方法分布 | 未提供：1 |
| 正式科研数据数量 | 0 |
| 原文件留存 | 入库字节与 CSV 完全一致 |
| 原文件 SHA-256 | `5338ab5ef488c277ddc12ad4bcc4c06c00db586ba61a09c7a7707f173bc0c1a5` |

再次实际执行相同导入命令，进程退出码为 2，报告 `duplicate` 错误；版本数量仍为 1、Demo 记录仍为 1、正式科研记录仍为 0。没有新增重复记录或空版本。

机器可读验收结果：`data/research/demo_import_verification.json`。完整原文件、第一次导入的检查报告和版本元数据保存在 `data/research/datasets.sqlite3`。在“科研数据管理”页面选择 Demo 仓库可查看、下载。

## 自动化检查

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m compileall -q data_manager pages scripts tests app.py
```

最终结果：**138 passed in 5.74s**；语法编译检查完成，无错误。

新增测试覆盖：

- CSV、GB18030 CSV、JSON 数组/单位映射、XLSX 工作表选择和文本编号；损坏的 CSV/JSON/XLSX/XLS 文件报错。
- 标准单位、单位列、单位对象、数值后缀、显式默认单位声明及换算；单位冲突、HV/HRC 不兼容、SI 大小写歧义拦截。
- 缺失值、0、布尔值、公式、非有限数、负数、气孔率范围、未知字段和重复列。
- 本批/跨批重复拦截、同值平行记录提示、错误批次整批回滚、并发同一编号只提交一次。
- 历史版本不变、累计与新增数量、不存在的版本报错、原文件字节还原。
- 字段完整度、材料/方法分布、筛选与分页、测试仓库隔离。
- 至少 8 个观测的统计异常提示，保留原值；问题详情截断时计数仍完整。
- 临时仓库中 **10,000 条无测量占位记录** 批量导入及末页读取。
- Streamlit AppTest：由主页进入科研页面，空仓库、Demo 仓库、历史版本切换；上传内容经文件控件替身送入，页面执行真实预览、提交和重复阻断逻辑。
- 标准化数据符合 JSON Schema；交付的静态 Schema 与代码定义一致。

所有算术/边界测试值只出现在测试代码和 pytest 临时目录，不写入交付的科研仓库。现有材料库、Agent 推荐与模型设置回归测试也通过。

## 验证边界

本次 Excel 成功导入验收使用 XLSX；旧 XLS 已接入 xlrd 并验证损坏文件报错，尚未用真实旧 XLS 工作簿完成成功导入验收。XLS 缓存数值不能自动判别其是否来自公式，使用时需核对来源。

页面完成 AppTest 功能验证，未执行人工浏览器文件选择器交互或截图验收。未做百万级数据和多用户长时间负载测试。异常提示属于数据复核，不代表工程判定；原始单位被错误标注但数值看似合理时，仍需核对原始台账。
